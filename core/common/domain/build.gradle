plugins {
    id 'java-library'
    alias(libs.plugins.kotlinJvm)
}
apply from: "$rootDir/plugins/kotlin-build.gradle"

java {
    sourceCompatibility = JavaVersion.VERSION_17
    targetCompatibility = JavaVersion.VERSION_17
}

dependencies {
    // Libs
    implementation(libs.coroutinesCore)
}
